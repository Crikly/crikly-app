import { ComingSoon } from '@/components/parent/ComingSoon'

// P-04-A: placeholder target for the ProfilePopover "Settings" row. The
// parent settings screen ships in a later parent-module step.

export default function ParentSettingsPage() {
  return (
    <ComingSoon
      title="Settings"
      copy="Account settings — your details, notifications and privacy controls — are coming very soon."
    />
  )
}
