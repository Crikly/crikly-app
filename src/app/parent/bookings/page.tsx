import { ComingSoon } from '@/components/parent/ComingSoon'

// P-04-A: placeholder target for the "My bookings" nav link and the
// "Past bookings" link on the dashboard. The bookings list ships in a
// later parent-module step.

export default function ParentBookingsPage() {
  return (
    <ComingSoon
      title="My bookings"
      copy="Your bookings will live here — upcoming sessions, past sessions and receipts. Coming very soon."
    />
  )
}
