import { ComingSoon } from '@/components/parent/ComingSoon'

// P-04-A: placeholder target for the + Add child bubble. The real
// add-child flow ships with P-07.

export default function AddChildPage() {
  return (
    <ComingSoon
      title="Add a child"
      copy="Adding child profiles is coming very soon. You'll be able to set up a profile for each child — with their sports, skill level and anything their coach should know."
    />
  )
}
