// STUB: built in MS-10
// Route: /splash
// Note: lives at /splash (not /) to avoid conflict with src/app/page.tsx redirect handler.
export default function RoleSplashPage() {
  return <div>Role Splash — MS-10</div>
}
